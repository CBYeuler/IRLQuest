Linux:

Download/clone the repo
Open terminal in the project folder
Run bash install.sh
Click the desktop shortcut from then on

Windows:

Download/clone the repo
Double-click install.bat
Click the desktop shortcut from then on
